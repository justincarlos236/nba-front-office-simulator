/**
 * Seeds reference data (teams, players, season stats) and bootstraps the
 * first League from that snapshot. Implemented in the data-pipeline
 * milestone — see docs/ROADMAP.md.
 */
async function main() {
  console.log("Seed pipeline not yet implemented — see docs/ROADMAP.md.");
}

main();
